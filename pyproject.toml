[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "neuron-steering"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["neuron_steer*"]